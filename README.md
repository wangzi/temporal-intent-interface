# z.stillinlove.co

A personal journal subdomain. Placeholder during initial setup; will be powered by `journalkit` once it lands.

## Local dev

```bash
pnpm install
pnpm dev
```

Opens at `http://localhost:3000`.

## Deploy

Auto-deploys from `main` via Vercel. Production URL: `https://z.stillinlove.co`.
